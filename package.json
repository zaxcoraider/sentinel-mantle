{
  "name": "sentinel-mantle",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:monitor": "pnpm --filter monitor dev",
    "build": "pnpm -r build",
    "vercel-build": "pnpm --filter sentinel-web build && cp -r web/.next .next",
    "test": "pnpm -r test",
    "gen:abis": "pnpm --filter web gen:abis"
  },
  "devDependencies": {
    "next": "^14.2.29"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
